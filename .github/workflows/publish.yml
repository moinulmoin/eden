name: Publish

on:
  push:
    tags:
      - "v*"

concurrency:
  group: npm-publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: .nvmrc
          registry-url: https://registry.npmjs.org

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.4.0

      - name: Validate release tag
        id: version
        run: |
          git merge-base --is-ancestor "$GITHUB_SHA" origin/main
          version="${GITHUB_REF_NAME#v}"
          node -e '
            const version = process.argv[1];
            if (!/^(?:0|[1-9]\d*)\.(?:0|[1-9]\d*)\.(?:0|[1-9]\d*)$/.test(version)) {
              throw new Error(`invalid release tag: v${version}`);
            }
          ' "$version"
          printf 'value=%s\n' "$version" >> "$GITHUB_OUTPUT"

      - name: Enable pinned pnpm
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run release gates
        run: |
          pnpm run build
          pnpm run typecheck
          pnpm run lint
          pnpm run test
          pnpm run conformance:local
        env:
          EDEN_OWNED_TERM_GRACE_MS: "5000"
          EDEN_PACKAGE_TEST_TIMEOUT_MS: "600000"
          EDEN_PACKAGE_TEST_SCRIPTS_TIMEOUT_MS: "900000"
          EDEN_WRANGLER_DRY_RUN_TIMEOUT_MS: "120000"
          EDEN_GENERATION_WORK_TIMEOUT_MS: "20000"
          EDEN_GENERATION_PUBLISH_TIMEOUT_MS: "4000"
          EDEN_RUNTIME_PROOF_TIMEOUT_MS: "30000"
          EDEN_RUNTIME_READY_TIMEOUT_MS: "30000"
          EDEN_RUNTIME_WATCHER_TIMEOUT_MS: "30000"

      - name: Prepare exact release tarballs
        run: >-
          node scripts/release-packages.mjs prepare
          "${{ steps.version.outputs.value }}" release-tarballs

      - name: Publish with npm trusted publishing
        run: >-
          node scripts/release-packages.mjs publish
          release-tarballs/release-manifest.json

      - name: Verify npm package graph
        run: >-
          node scripts/release-packages.mjs verify
          release-tarballs/release-manifest.json

      - name: Smoke supported installers
        env:
          VERSION: ${{ steps.version.outputs.value }}
        run: |
          npx --yes --package="@moinulmoin/eden@$VERSION" eden --help
          pnpm dlx "@moinulmoin/eden@$VERSION" --help
          bunx --package "@moinulmoin/eden@$VERSION" eden --help

  release:
    needs: publish
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: >-
          gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1 ||
          gh release create "$GITHUB_REF_NAME" --verify-tag --generate-notes
